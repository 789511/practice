<!DOCTYPE html>
<html>
  <head>
    <title>Games Marketplace</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Маркетплейс видеоигр с огромным выбором игр для всех платформ.">
    <meta name="keywords" content="видеоигры, маркетплейс, игры, купить игры">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="#">Видеоигры Маркетплейс</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
    
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item">
                            <%= link_to 'Игры', games_path, class: "nav-link #{'active' if current_page?(games_path)}" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to 'О нас', about_path, class: "nav-link #{'active' if current_page?(about_path)}" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to 'Контакты', contact_path, class: "nav-link #{'active' if current_page?(contact_path)}" %>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Профиль</a>
                        </li>
                    </ul>
                    <form class="d-flex my-2 my-lg-0 ms-3">
                        <input class="form-control me-2" type="search" placeholder="" aria-label="Search">
                        <button class="btn btn-purple" type="submit">Найти</button>
                    </form>
                </div>
            </div>
        </nav>
    </header>

    <div class="container mt-2">
      <div class="row">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
